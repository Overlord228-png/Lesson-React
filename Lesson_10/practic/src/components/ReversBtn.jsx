import React from 'react'

export default function ReversBtn(props) {
    return (
        <div>
            <button onClick={()=>props.ReneamUser()}>
                revers
            </button>
        </div>
    )
}
